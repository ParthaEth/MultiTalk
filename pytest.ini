[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -q
    --tb=short
    --strict-markers
markers =
    unit: fast unit tests with no heavy model execution
    worker_node: deeper checks that require GPU worker dependencies and model assets
